# 1. 보안 및 개인 설정 파일 (가장 중요!)
.env
google_key.json
browser_profile/
credentials/

# 2. 가상환경 및 Python 관련
venv/
.venv/             # [추가] 스크린샷에 보이는 가상환경 폴더명 (.venv)
__pycache__/
*.py[cod]

# 3. 결과물 및 미디어 데이터 (용량이 커서 제외)
results/
images/
audio/
video/
assets/

# 4. 임시 미디어 파일 (루트 경로에 생기는 것들 방지)
*.mp3              # [추가] 스크린샷의 output_gcp.mp3 같은 파일 제외
*.mp4              # [추가] 혹시 루트에 생성될지 모르는 영상 제외

# 5. 운영체제 및 실행 스크립트
.DS_Store
Thumbs.db
*.bat              # [수정] run_cinema.bat 뿐만 아니라 run_scheduler.bat도 포함